% file: sections/mst-sp.tex

%%%%%%%%%%%%%
\begin{frame}{}
  \begin{exampleblock}{MST vs. Shortest Paths \pno{$10.15 \; (6)$}}
    \centerline{\xmark The shortest path between two nodes is necessarily part of some MST.}
  \end{exampleblock}

  \fignocaption{width = 0.10\textwidth}{figs/mst-sssp.pdf}
\end{frame}
%%%%%%%%%%%%%

%%%%%%%%%%%%%
\begin{frame}{}
  \begin{exampleblock}{Sharing edges \pno{3.6.5}}
    \begin{itemize}
      \item $G = (V, E), w(e) > 0$
      \item Given $s$: all sssp trees from $s$ must share some edge with \textcolor{red}{all} (\textcolor{gray}{some}) MSTs of $G$
    \end{itemize}
      \cmark $w > 0$; Vertex $s$; shortest-path tree of $s$ and some MST share a common edge \pno{$10.9$}
  \end{exampleblock}

  \begin{block}{Solution}
    $E'$: lightest edges leaving $s$
    \begin{itemize}
      \item any MST $T$ of $G$: $T \cap E' \neq \emptyset$
      \item $E' \subset \forall \text{ sssp trees}$
    \end{itemize}
  \end{block}
\end{frame}
%%%%%%%%%%%%%
